[project]
name = "docsrag"
version = "0.1.0"
description = "Retrieval-Augmented Documentation Assistant"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
